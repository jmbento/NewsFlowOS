# 🚀 COMANDO CLI PARA INICIAR SERVIDOR

## ⚡ Execute ESTE comando no seu Terminal:

```bash
cd /Volumes/bxdMAC/Projetos-apps/newsflow-nodes && python3 server.py
```

## 🌐 OU use o script:

```bash
cd /Volumes/bxdMAC/Projetos-apps/newsflow-nodes
./START_SERVER_CLI.sh
```

## 📱 Acesse:

**http://localhost:3000**

---

## 🔄 Alternativa: Servidor Python Simples (sem arquivos extras)

Se o `server.py` não funcionar, use este comando direto:

```bash
cd /Volumes/bxdMAC/Projetos-apps/newsflow-nodes && python3 -m http.server 3000
```

---

## 🎯 Para Reunião - Comando Único:

```bash
cd /Volumes/bxdMAC/Projetos-apps/newsflow-nodes && python3 -m http.server 3000
```

Depois acesse: **http://localhost:3000**

---

**⚠️ Nota:** Este servidor serve arquivos estáticos. Para desenvolvimento completo com hot-reload, use `npm run dev` no seu terminal (fora do Cursor).
